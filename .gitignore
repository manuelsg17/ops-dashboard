# Node / Vite (build step — Fase A1)
node_modules/
dist/

# Claude Code internal files
.claude/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Entorno local (Supabase en Docker) — NUNCA commitear
.env
.env.local
.env.*.local

# Artefactos de trabajo con datos REALES de negocio (CLIDs, metas, db_ids).
# Nunca se commitean: el repo es publico en GitHub Pages.
/backups/
/*.xls
/*.xlsx
/top5_por_ciudad_dbids.csv
